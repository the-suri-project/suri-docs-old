import{_ as e,o as t,c as a,a as o}from"./app.2bcd877f.js";const m=JSON.parse('{"title":"Tagged suris","description":"","frontmatter":{},"headers":[],"relativePath":"en/domains/tagged.md"}'),s={name:"en/domains/tagged.md"},i=o('<h1 id="tagged-suris" tabindex="-1">Tagged suris <a class="header-anchor" href="#tagged-suris" aria-hidden="true">#</a></h1><p>Tagged suris consist of a prefix denoting a special meaning and a list of suri names separated by a dot <code>.</code>, e.g. <code>@myUserName.twitter</code>, <code>#subject</code>, <code>$USDC</code>, etc.</p><div class="info custom-block"><p class="custom-block-title">Insight</p><p>Tagged domains are just a special representation of an equivalent canonical suri, thus they do not have any special meaning to the system or added functionality.</p></div><p>The idea behind this representation is to emphasize an identity or a subject, which is more relevant than the rest of the suri. This is especially useful for suris that belong to users or brands.</p><p>Some examples are:</p><ul><li><p>We want to use a suri as a source of our digital identity, we can use a <em>mention</em> <code>@username</code> and then use subdomains to split the content for our social networks, e.g. <code>@username.twitter</code>, <code>@username.facebook</code>, etc.</p></li><li><p>A Web3 project is minting a new token and exposing its data using a <em>cashtag</em> <code>$MY_TOKEN</code>. They want to store its public information, like where to buy it, the project&#39;s website..., in the suri&#39;s records but storing the pricing information in <code>$MY_TOKEN.pricing</code> to separate the concepts and using the suri as a kind of oracle.</p></li></ul><p>If instead of using a tagged suri we used a regular suri, we would have ended with <code>twitter.username</code> or <code>pricing.MY_TOKEN</code> which are perfectly fine but do not emphasize the actual subject of the action.</p><p>You can check the full <a href="/en/domains/formats#tagged-format">definition</a> and all tagged types in the following section.</p>',8),n=[i];function r(c,d,u,p,l,h){return t(),a("div",null,n)}const f=e(s,[["render",r]]);export{m as __pageData,f as default};
